name: CI/CD - Trivy Scan Python Apps

on:
  push:
    branches:
      - main
      - ci-cd-demo-test

jobs:
  trivy-scan:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        app: [app-sec, app-vuln]

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        run: |
          docker build -t ${{ matrix.app }} -f ${{ matrix.app }}/Dockerfile ${{ matrix.app }}

      - name: Install Trivy
        run: |
          curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sudo sh -s -- -b /usr/local/bin

      - name: Check Trivy
        run: trivy --version

      - name: Scan image with Trivy
        run: |
          if [ "${{ matrix.app }}" = "app-vuln" ]; then
            echo "Scanning vulnerable app..."
            trivy image --exit-code 1 --severity HIGH,CRITICAL ${{ matrix.app }}
          else
            echo "Scanning secure app..."
            trivy image --exit-code 0 --severity HIGH,CRITICAL ${{ matrix.app }}
          fi

      - name: Log in to Docker Hub
        if: ${{ success() }}
        uses: docker/login-action@v2
        with:
          username: sdformit
          password: ${{ secrets.SDFORMIT }}

      - name: Push image to Docker Hub
        if: ${{ success() }}
        run: |
          docker tag ${{ matrix.app }} sdformit/${{ matrix.app }}:latest
          docker push sdformit/${{ matrix.app }}:latest

